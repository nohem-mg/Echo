# Root env template — contracts (Foundry) only.
#
# Everything else lives in exactly two files:
#   backend values  → backend/.env.example   (copy to backend/.env)
#   frontend values → frontend/.env.example  (copy to frontend/.env.local)

# ── Contracts (contracts/, forge script / verify) ────────────────────────────
SEPOLIA_RPC_URL=
CRE_CALLER_ADDRESS=
ESCROW_REGISTRY=
ESCROW_TOKEN=
ETHERSCAN_API_KEY=
DEPLOYER_PRIVATE_KEY=
